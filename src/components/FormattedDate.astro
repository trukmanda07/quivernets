---
interface Props {
	date: Date | string;
}

const { date } = Astro.props;
// Ensure we have a Date object (handle both Date and string)
const dateObj = date instanceof Date ? date : new Date(date);
---

<time datetime={dateObj.toISOString()}>
	{
		dateObj.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
